# Grafana dashboard provider：自动加载 /var/lib/grafana/dashboards 下的 JSON
apiVersion: 1

providers:
  - name: 'gofile'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
